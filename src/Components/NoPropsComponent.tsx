// NoPropsComponent.tsx

import React from "react";

const NoPropsComponent: React.FC = () => {
  return (
    <div>
      <p>This component doesn't require any props.</p>
    </div>
  );
};

export default NoPropsComponent;
